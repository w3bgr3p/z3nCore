# Документация класса Sql

Класс на основе экземпляра, предоставляющий унифицированные операции с базами данных для SQLite и PostgreSQL с автоматическим определением.

---

## Конструктор

### Sql
**Назначение**: Создает новый экземпляр Sql с автоматическим определением режима базы данных.

**Пример**:
```csharp
// Создать экземпляр с логированием
var sql = new Sql(project, log: true);

// Без логирования
var sql = new Sql(project);
```

**Разбор**:
```csharp
public Sql(
    IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    bool log = false                   // Включить логирование с эмодзи для конкретной базы данных
)
// Автоматически определяет режим базы данных из переменной DBmode
// Настраивает логгер с эмодзи: 🐘 для PostgreSQL, ✒ для SQLite
// Читает пароль из переменной DBpstgrPass
```

---

## Публичные методы

### MkTable
**Назначение**: Создает или обновляет таблицу с автоматическим управлением колонками.

**Пример**:
```csharp
var sql = new Sql(project);
var structure = new Dictionary<string, string> {
    {"id", "INTEGER PRIMARY KEY"},
    {"email", "TEXT DEFAULT ''"},
    {"status", "TEXT DEFAULT ''"}
};

sql.MkTable(structure, "users");

// Строгий режим удаляет лишние колонки
sql.MkTable(structure, "users", strictMode: true);
```

**Разбор**:
```csharp
public void MkTable(
    Dictionary<string, string> tableStructure,  // Определения колонок
    string tableName = null,                    // Имя таблицы (по умолчанию: из настроек проекта)
    bool strictMode = false,                    // Удалить колонки, не входящие в структуру
    bool insertData = false,                    // Вставить начальный диапазон данных
    string host = "localhost:5432",             // Хост PostgreSQL
    string dbName = "postgres",                 // Имя базы данных
    string dbUser = "postgres",                 // Имя пользователя
    string dbPswd = "",                         // Пароль
    string schemaName = "projects",             // Схема PostgreSQL
    bool log = false                            // Включить логирование
)
// Работает как с SQLite, так и с PostgreSQL
// Автоматически преобразует типы для целевой базы данных
// Использует переменную makeTable для определения, должна ли создаваться таблица
```

---

### Write
**Назначение**: Записывает пары ключ-значение в таблицу с логикой upsert.

**Пример**:
```csharp
var sql = new Sql(project);
var data = new Dictionary<string, string> {
    {"api_key", "abc123"},
    {"api_secret", "secret456"}
};

sql.Write(data, "settings");
```

**Разбор**:
```csharp
public void Write(
    Dictionary<string, string> toWrite,  // Пары ключ-значение для записи
    string tableName = null,             // Имя таблицы (по умолчанию: переменная projectTable)
    bool log = false,                    // Включить логирование
    bool throwOnEx = false,              // Выбрасывать исключения при true
    bool last = true                     // Обновить колонку last с временной меткой
)
// Использует ON CONFLICT для обновления существующих записей
// Экранирует одинарные кавычки в ключах и значениях
// Каждая пара ключ-значение - это отдельный INSERT/UPDATE
```

---

### Upd (строковая версия)
**Назначение**: Обновляет записи базы данных выражением SQL update.

**Пример**:
```csharp
var sql = new Sql(project);

// Обновление по id
sql.Upd("status = 'completed', result = 'success'", "users");

// Пользовательское условие WHERE
sql.Upd("status = 'active'", "users", where: "status = 'pending'");

// Обновление по пользовательскому ключу
sql.Upd("email = 'new@example.com'", "users", key: "username", acc: "john");
```

**Разбор**:
```csharp
public void Upd(
    string toUpd,               // Выражение обновления (например, "col = 'value'")
    string tableName = null,    // Имя таблицы (по умолчанию: переменная projectTable)
    bool log = false,           // Включить логирование
    bool throwOnEx = false,     // Выбрасывать исключения при true
    bool last = true,           // Автоматически обновлять колонку last
    string key = "id",          // Имя ключевой колонки
    object acc = null,          // Значение ключа (по умолчанию: переменная acc0)
    string where = ""           // Пользовательское условие WHERE
)
// Автоматически добавляет временную метку в колонку last, если last=true
// Экранирует имена колонок для безопасности
// Пропускает обновление last для специальных таблиц (blockchain, browser и т.д.)
```

---

### Upd (версия Dictionary)
**Назначение**: Обновляет базу данных, используя словарь пар колонка-значение.

**Пример**:
```csharp
var sql = new Sql(project);
var updates = new Dictionary<string, string> {
    {"email", "test@example.com"},
    {"status", "active"}
};

sql.Upd(updates, "users");
```

**Разбор**:
```csharp
public void Upd(
    Dictionary<string, string> toWrite,  // Пары колонка-значение
    string tableName = null,             // Имя таблицы
    bool log = false,                    // Включить логирование
    bool throwOnEx = false,              // Выбрасывать исключения при true
    bool last = true,                    // Обновить колонку last
    string key = "id",                   // Имя ключевой колонки
    object acc = null,                   // Значение ключа
    string where = ""                    // Пользовательское условие WHERE
)
// Преобразует словарь в выражение SQL update
// Вызывает строковую версию метода Upd
```

---

### Upd (версия List)
**Назначение**: Обновляет таблицу списком значений для определенной колонки.

**Пример**:
```csharp
var sql = new Sql(project);
var emails = new List<string> {
    "user1@example.com",
    "user2@example.com",
    "user3@example.com"
};

sql.Upd(emails, "email", "users");
```

**Разбор**:
```csharp
public void Upd(
    List<string> toWrite,     // Список значений для записи
    string columnName,        // Имя колонки для обновления
    string tableName = null,  // Имя таблицы
    bool log = false,         // Включить логирование
    bool throwOnEx = false,   // Выбрасывать исключения при true
    bool last = true,         // Обновить колонку last
    bool byKey = false        // Зарезервированный параметр
)
// Автоматически добавляет диапазон для вмещения размера списка
// Обновляет последовательно по id (1, 2, 3, ...)
// Экранирует одинарные кавычки в значениях
```

---

### Get
**Назначение**: Извлекает данные из базы данных.

**Пример**:
```csharp
var sql = new Sql(project);

// Получить одно значение
string email = sql.Get("email", "users");

// Получить несколько колонок
string data = sql.Get("email, username", "users");

// Пользовательское WHERE
string email = sql.Get("email", "users", where: "status = 'active'");
```

**Разбор**:
```csharp
public string Get(
    string toGet,            // Имена колонок через запятую
    string tableName = null, // Имя таблицы (по умолчанию: переменная projectTable)
    bool log = false,        // Включить логирование
    bool throwOnEx = false,  // Выбрасывать исключения при true
    string key = "id",       // Имя ключевой колонки
    string acc = null,       // Значение ключа (по умолчанию: переменная acc0)
    string where = ""        // Пользовательское условие WHERE
)
// Возвращает: Отформатированную строку с разделителем колонок
// Автоматически экранирует имена колонок
// Выбрасывает: ArgumentException если toGet пуст
```

---

### GetRandom
**Назначение**: Извлекает случайное непустое значение из базы данных.

**Пример**:
```csharp
var sql = new Sql(project);

// Получить случайный прокси
string proxy = sql.GetRandom("proxy");

// Получить несколько случайных значений
string proxies = sql.GetRandom("proxy", single: false);

// Включить id в результат
string data = sql.GetRandom("proxy", acc: true);
```

**Разбор**:
```csharp
public string GetRandom(
    string toGet,            // Имя колонки
    string tableName = null, // Имя таблицы
    bool log = false,        // Включить логирование
    bool acc = false,        // Включить колонку id в результат
    bool throwOnEx = false,  // Выбрасывать исключения при true
    int range = 0,           // Максимальный id для рассмотрения (по умолчанию: из переменной range)
    bool single = true,      // Вернуть одну строку или несколько
    bool invert = false      // Получить пустые значения вместо непустых
)
// Возвращает: Случайное значение, используя ORDER BY RANDOM()
// Фильтрует пустые значения (если только invert=true)
// Ограничивает по range, чтобы избежать сканирования всей таблицы
```

---

### GetColumns
**Назначение**: Получает список имен колонок через запятую.

**Пример**:
```csharp
var sql = new Sql(project);
string columns = sql.GetColumns("users");
// Результат: "id, email, username, status"
```

**Разбор**:
```csharp
public string GetColumns(
    string tableName,  // Имя таблицы
    bool log = false   // Включить логирование
)
// Возвращает: Имена колонок через запятую
// Запрашивает information_schema для PostgreSQL
// Запрашивает pragma_table_info для SQLite
```

---

### GetColumnList
**Назначение**: Получает список имен колонок для таблицы.

**Пример**:
```csharp
var sql = new Sql(project);
List<string> columns = sql.GetColumnList("users");
foreach (var col in columns)
{
    project.SendInfoToLog(col, true);
}
```

**Разбор**:
```csharp
public List<string> GetColumnList(
    string tableName,  // Имя таблицы
    bool log = false   // Включить логирование
)
// Возвращает: Список имен колонок
// Полезно для итерации или обработки
```

---

### TblExist
**Назначение**: Проверяет, существует ли таблица.

**Пример**:
```csharp
var sql = new Sql(project);
if (sql.TblExist("users"))
{
    // Таблица существует
}
```

**Разбор**:
```csharp
public bool TblExist(
    string tblName  // Имя таблицы для проверки
)
// Возвращает: true если таблица существует, иначе false
```

---

### TblAdd
**Назначение**: Создает новую таблицу, если она не существует.

**Пример**:
```csharp
var sql = new Sql(project);
var structure = new Dictionary<string, string> {
    {"id", "INTEGER PRIMARY KEY"},
    {"email", "TEXT DEFAULT ''"}
};
sql.TblAdd("users", structure);
```

**Разбор**:
```csharp
public void TblAdd(
    string tblName,                             // Имя таблицы
    Dictionary<string, string> tableStructure   // Определения колонок
)
// Не делает ничего, если таблица уже существует
// Автоматически преобразует AUTOINCREMENT в SERIAL для PostgreSQL
```

---

### ClmnExist
**Назначение**: Проверяет, существует ли колонка в таблице.

**Пример**:
```csharp
var sql = new Sql(project);
if (sql.ClmnExist("users", "email"))
{
    // Колонка существует
}
```

**Разбор**:
```csharp
public bool ClmnExist(
    string tblName,  // Имя таблицы
    string clmnName  // Имя колонки
)
// Возвращает: true если колонка существует, иначе false
// Регистронезависимо для PostgreSQL
```

---

### ClmnAdd (одна колонка)
**Назначение**: Добавляет колонку в таблицу, если она не существует.

**Пример**:
```csharp
var sql = new Sql(project);
sql.ClmnAdd("users", "phone_number");

// С пользовательским типом
sql.ClmnAdd("users", "age", "INTEGER DEFAULT 0");
```

**Разбор**:
```csharp
public void ClmnAdd(
    string tblName,                          // Имя таблицы
    string clmnName,                         // Имя колонки
    string defaultValue = "TEXT DEFAULT ''"  // Тип колонки и значение по умолчанию
)
// Не делает ничего, если колонка уже существует
```

---

### ClmnAdd (Dictionary)
**Назначение**: Добавляет несколько колонок из словаря.

**Пример**:
```csharp
var sql = new Sql(project);
var structure = new Dictionary<string, string> {
    {"email", "TEXT DEFAULT ''"},
    {"phone", "TEXT DEFAULT ''"}
};
sql.ClmnAdd("users", structure);
```

**Разбор**:
```csharp
public void ClmnAdd(
    string tblName,                             // Имя таблицы
    Dictionary<string, string> tableStructure   // Колонки для добавления
)
// Добавляет только колонки, которые не существуют
// Логирует какие колонки добавляются
```

---

### AddRange
**Назначение**: Вставляет записи ID в таблицу.

**Пример**:
```csharp
var sql = new Sql(project);
sql.AddRange("users", 100);  // Добавляет ID 1-100
```

**Разбор**:
```csharp
public void AddRange(
    string tblName,  // Имя таблицы
    int range = 0    // Максимальный ID (по умолчанию: переменная rangeEnd)
)
// Вставляет отсутствующие ID от (текущий макс + 1) до range
// Использует ON CONFLICT DO NOTHING
// По умолчанию 100, если rangeEnd не установлен
```

---

### Settings
**Назначение**: Загружает настройки из таблицы _settings.

**Пример**:
```csharp
var sql = new Sql(project);

// Загрузить и установить переменные
var config = sql.Settings();

// Загрузить без установки переменных
var config = sql.Settings(set: false);
```

**Разбор**:
```csharp
public Dictionary<string, string> Settings(
    bool set = true  // Установить переменные проекта из настроек
)
// Возвращает: Словарь настроек
// Читает пары ключ-значение из таблицы _settings
// Опционально устанавливает переменные проекта
```

---

### Address
**Назначение**: Извлекает адрес блокчейна для указанной сети.

**Пример**:
```csharp
var sql = new Sql(project);

// Получить адрес EVM
string evmAddr = sql.Address("evm");

// Получить адрес Solana
string solAddr = sql.Address("sol");
```

**Разбор**:
```csharp
public string Address(
    string chainType = "evm"  // Имя колонки типа сети
)
// Возвращает: Адрес из таблицы _addresses
// Устанавливает переменную проекта address{CHAINTYPE} с результатом
// Читает из текущего аккаунта (переменная acc0)
```

---

### Key
**Назначение**: Извлекает криптографический приватный ключ с опциональной расшифровкой.

**Пример**:
```csharp
var sql = new Sql(project);

// Получить приватный ключ EVM
string evmKey = sql.Key("evm");

// Получить ключ Solana
string solKey = sql.Key("sol");

// Получить seed фразу
string seed = sql.Key("seed");
```

**Разбор**:
```csharp
public string Key(
    string chainType = "evm"  // Тип ключа: evm, sol или seed
)
// Поддерживаемые типы:
//   "evm" -> колонка secp256k1
//   "sol" -> колонка base58
//   "seed" -> колонка bip39
// Расшифровывает, используя переменную cfgPin, если установлена
// Выбрасывает: Exception для неподдерживаемого chainType
```

---

## Публичные поля

### columnsDefault
Колонки по умолчанию для стандартных таблиц.

```csharp
public string[] columnsDefault = {
    "status",
    "last",
};
```

### columnsSocial
Стандартные колонки для таблиц социальных сетей.

```csharp
public string[] columnsSocial = {
    "status",
    "last",
    "cookie",
    "login",
    "pass",
    "otpsecret",
    "email",
    "recovery",
};
```

---
