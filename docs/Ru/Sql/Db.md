# Документация класса Db

Статический класс, предоставляющий высокоуровневые операции с базой данных для проектов ZennoPoster с поддержкой SQLite и PostgreSQL.

---

## Публичные методы

### DbGet
**Назначение**: Извлекает одно значение из таблицы базы данных.

**Пример**:
```csharp
// Получить значение прокси для текущего аккаунта
string proxy = project.DbGet("proxy");

// Получить конкретную колонку с пользовательским ключом
string email = project.DbGet("email", "users", key: "user_id", acc: "123");
```

**Разбор**:
```csharp
public static string DbGet(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    string toGet,                           // Имя колонки для получения
    string tableName = null,                // Имя таблицы (по умолчанию: переменная projectTable)
    bool log = false,                       // Включить логирование
    bool thrw = false,                      // Выбрасывать исключения при true
    string key = "id",                      // Имя ключевой колонки
    string acc = null,                      // ID аккаунта (по умолчанию: переменная acc0)
    string where = ""                       // Пользовательское условие WHERE
)
// Возвращает: Строковое значение из базы данных
```

---

### DbGetColumns
**Назначение**: Извлекает строку из базы данных в виде словаря с именами колонок в качестве ключей.

**Пример**:
```csharp
// Получить несколько колонок для текущего аккаунта
var userData = project.DbGetColumns("email, username, status");

// Доступ к отдельным значениям
string email = userData["email"];
string username = userData["username"];
```

**Разбор**:
```csharp
public static Dictionary<string, string> DbGetColumns(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    string toGet,                           // Имена колонок через запятую
    string tableName = null,                // Имя таблицы (по умолчанию: переменная projectTable)
    bool log = false,                       // Включить логирование
    bool thrw = false,                      // Выбрасывать исключения при true
    string key = "id",                      // Имя ключевой колонки
    object id = null,                       // ID записи (по умолчанию: переменная acc0)
    string where = ""                       // Пользовательское условие WHERE
)
// Возвращает: Словарь с именами колонок в качестве ключей и значениями в виде строк
```

---

### DbGetLine
**Назначение**: Извлекает строку из базы данных в виде массива значений.

**Пример**:
```csharp
// Получить несколько колонок как массив
string[] userData = project.DbGetLine("email, username, status");

// Доступ по индексу
string email = userData[0];
string username = userData[1];
```

**Разбор**:
```csharp
public static string[] DbGetLine(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    string toGet,                           // Имена колонок через запятую
    string tableName = null,                // Имя таблицы (по умолчанию: переменная projectTable)
    bool log = false,                       // Включить логирование
    bool thrw = false,                      // Выбрасывать исключения при true
    string key = "id",                      // Имя ключевой колонки
    object id = null,                       // ID записи (по умолчанию: переменная acc0)
    string where = ""                       // Пользовательское условие WHERE
)
// Возвращает: Массив строковых значений
```

---

### DbGetLines
**Назначение**: Извлекает несколько строк из базы данных в виде списка строк.

**Пример**:
```csharp
// Получить все email адреса
var emails = project.DbGetLines("email", where: "status = 'active'");

// Синхронизация со списком ZennoPoster
var emails = project.DbGetLines("email", toList: "emailList");
```

**Разбор**:
```csharp
public static List<string> DbGetLines(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    string toGet,                           // Имя колонки для получения
    string tableName = null,                // Имя таблицы (по умолчанию: переменная projectTable)
    bool log = false,                       // Включить логирование
    bool thrw = false,                      // Выбрасывать исключения при true
    string key = "id",                      // Имя ключевой колонки
    object id = null,                       // ID записи (по умолчанию: переменная acc0)
    string where = "",                      // Пользовательское условие WHERE
    string toList = null                    // Имя списка ZennoPoster для синхронизации результатов
)
// Возвращает: Список строковых значений
```

---

### DbToVars
**Назначение**: Извлекает строку базы данных и автоматически устанавливает переменные ZennoPoster.

**Пример**:
```csharp
// Получить колонки и установить как переменные
project.DbToVars("email, username, proxy");

// Теперь переменные доступны
string email = project.Variables["email"].Value;
```

**Разбор**:
```csharp
public static Dictionary<string, string> DbToVars(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    string toGet,                           // Имена колонок через запятую
    string tableName = null,                // Имя таблицы (по умолчанию: переменная projectTable)
    bool log = false,                       // Включить логирование
    bool thrw = false,                      // Выбрасывать исключения при true
    string key = "id",                      // Имя ключевой колонки
    object id = null,                       // ID записи (по умолчанию: переменная acc0)
    string where = ""                       // Пользовательское условие WHERE
)
// Возвращает: Словарь полученных данных
// Побочный эффект: Устанавливает переменные проекта с именами колонок
```

---

### JsonToDb
**Назначение**: Парсит JSON и записывает данные в базу данных.

**Пример**:
```csharp
string json = "{\"email\":\"test@example.com\",\"status\":\"active\"}";
project.JsonToDb(json, "users");
```

**Разбор**:
```csharp
public static void JsonToDb(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    string json,                            // JSON строка для парсинга
    string tableName = null,                // Имя таблицы (по умолчанию: переменная projectTable)
    bool log = false,                       // Включить логирование
    bool thrw = false                       // Выбрасывать исключения при true
)
// Преобразует JSON в словарь и обновляет базу данных
```

---

### DicToDb
**Назначение**: Записывает данные словаря в базу данных.

**Пример**:
```csharp
var data = new Dictionary<string, string> {
    {"email", "test@example.com"},
    {"status", "active"}
};
project.DicToDb(data, "users");
```

**Разбор**:
```csharp
public static void DicToDb(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    Dictionary<string,string> dataDic,      // Данные для записи
    string tableName = null,                // Имя таблицы (по умолчанию: переменная projectTable)
    bool log = false,                       // Включить логирование
    bool thrw = false                       // Выбрасывать исключения при true
)
// Автоматически добавляет отсутствующие колонки и обновляет базу данных
```

---

### DbUpd
**Назначение**: Обновляет запись базы данных указанными значениями.

**Пример**:
```csharp
// Обновить одну колонку
project.DbUpd("status = 'completed'");

// Обновить несколько колонок
project.DbUpd("email = 'new@example.com', status = 'active'");

// Пользовательское условие WHERE
project.DbUpd("status = 'processed'", where: "status = 'pending'");
```

**Разбор**:
```csharp
public static void DbUpd(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    string toUpd,                           // Выражение обновления (например, "col = 'value'")
    string tableName = null,                // Имя таблицы (по умолчанию: переменная projectTable)
    bool log = false,                       // Включить логирование
    bool thrw = false,                      // Выбрасывать исключения при true
    string key = "id",                      // Имя ключевой колонки
    object acc = null,                      // ID аккаунта (по умолчанию: переменная acc0)
    string where = ""                       // Пользовательское условие WHERE
)
// Обновляет запись базы данных и сохраняет запрос в переменной lastQuery
```

---

### DbSettings
**Назначение**: Загружает настройки из таблицы _settings и опционально устанавливает переменные проекта.

**Пример**:
```csharp
// Загрузить и установить переменные
project.DbSettings();

// Загрузить без установки переменных
project.DbSettings(set: false, log: true);
```

**Разбор**:
```csharp
public static void DbSettings(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    bool set = true,                        // Установить переменные проекта из настроек
    bool log = false                        // Включить логирование
)
// Читает id и value из таблицы _settings
// Устанавливает переменные проекта когда set=true
```

---

### MigrateTable
**Назначение**: Копирует структуру и данные таблицы в новую таблицу, переименовывая устаревшие колонки.

**Пример**:
```csharp
// Мигрировать старую таблицу в новую структуру
project.MigrateTable("old_users", "users");
```

**Разбор**:
```csharp
public static void MigrateTable(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    string source,                          // Имя исходной таблицы
    string dest                             // Имя целевой таблицы
)
// Валидирует имена таблиц (только буквы, цифры и подчеркивания)
// Копирует все данные из источника в назначение
// Переименовывает колонки acc0/key в id, если они существуют
// Выбрасывает: ArgumentException если имена таблиц недопустимы
```

---

### DbGetRandom
**Назначение**: Извлекает случайное непустое значение из базы данных.

**Пример**:
```csharp
// Получить случайный прокси
string proxy = project.DbGetRandom("proxy");

// Получить несколько случайных значений
string proxies = project.DbGetRandom("proxy", single: false);

// Получить случайное пустое значение (инвертировать логику)
string empty = project.DbGetRandom("email", invert: true);
```

**Разбор**:
```csharp
public static string DbGetRandom(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    string toGet,                           // Имя колонки для получения
    string tableName = null,                // Имя таблицы (по умолчанию: переменная projectTable)
    bool log = false,                       // Включить логирование
    bool acc = false,                       // Включить колонку id в результат
    bool thrw = false,                      // Выбрасывать исключения при true
    int range = 0,                          // Максимальный ID для рассмотрения (по умолчанию: из переменной range)
    bool single = true,                     // Вернуть одно значение или несколько
    bool invert = false                     // Получить пустые значения вместо непустых
)
// Возвращает: Случайное значение из базы данных
// Использует ORDER BY RANDOM() для выбора
```

---

### DbKey
**Назначение**: Извлекает криптографический ключ из таблицы _wallets с опциональной расшифровкой.

**Пример**:
```csharp
// Получить приватный ключ EVM
string evmKey = project.DbKey("evm");

// Получить ключ Solana
string solKey = project.DbKey("sol");

// Получить seed фразу
string seed = project.DbKey("seed");
```

**Разбор**:
```csharp
public static string DbKey(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    string chainType = "evm"                // Тип блокчейна: evm, sol или seed
)
// Поддерживаемые значения chainType:
//   "evm" -> извлекает колонку secp256k1
//   "sol" -> извлекает колонку base58
//   "seed" -> извлекает колонку bip39
// Расшифровывает значение если установлена переменная cfgPin
// Выбрасывает: Exception для неподдерживаемого chainType
```

---

### TblAdd
**Назначение**: Создает новую таблицу, если она не существует.

**Пример**:
```csharp
var structure = new Dictionary<string, string> {
    {"id", "INTEGER PRIMARY KEY"},
    {"email", "TEXT DEFAULT ''"},
    {"status", "TEXT DEFAULT ''"}
};
project.TblAdd(structure, "users");
```

**Разбор**:
```csharp
public static void TblAdd(
    this IZennoPosterProjectModel project,       // Экземпляр проекта ZennoPoster
    Dictionary<string, string> tableStructure,   // Определения колонок
    string tblName,                              // Имя таблицы
    bool log = false                             // Включить логирование
)
// Не делает ничего, если таблица уже существует
// Автоматически преобразует AUTOINCREMENT в SERIAL для PostgreSQL
// Создает экранированные имена колонок для безопасности
```

---

### TblExist
**Назначение**: Проверяет, существует ли таблица в базе данных.

**Пример**:
```csharp
if (project.TblExist("users"))
{
    // Таблица существует
}
```

**Разбор**:
```csharp
public static bool TblExist(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    string tblName,                         // Имя таблицы для проверки
    bool log = false                        // Включить логирование
)
// Возвращает: true если таблица существует, иначе false
// Запрашивает information_schema для PostgreSQL, sqlite_master для SQLite
```

---

### TblList
**Назначение**: Извлекает список всех таблиц в базе данных.

**Пример**:
```csharp
var tables = project.TblList();
foreach (var table in tables)
{
    project.SendInfoToLog(table, true);
}
```

**Разбор**:
```csharp
public static List<string> TblList(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    bool log = false                        // Включить логирование
)
// Возвращает: Список имен таблиц в алфавитном порядке
// Исключает системные таблицы
```

---

### TblColumns
**Назначение**: Извлекает список имен колонок для таблицы.

**Пример**:
```csharp
var columns = project.TblColumns("users");
foreach (var column in columns)
{
    project.SendInfoToLog(column, true);
}
```

**Разбор**:
```csharp
public static List<string> TblColumns(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    string tblName,                         // Имя таблицы
    bool log = false                        // Включить логирование
)
// Возвращает: Список имен колонок
// Запрашивает information_schema для PostgreSQL, pragma_table_info для SQLite
```

---

### TblForProject
**Назначение**: Создает определение структуры таблицы для проекта со стандартными колонками.

**Пример**:
```csharp
// Базовая структура
var structure = project.TblForProject();

// С пользовательскими колонками
var columns = new List<string> { "email", "username", "proxy" };
var structure = project.TblForProject(columns);
```

**Разбор**:
```csharp
public static Dictionary<string, string> TblForProject(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    List<string> projectColumns = null,     // Пользовательские колонки для включения
    string defaultType = "TEXT DEFAULT ''"  // Тип колонки по умолчанию
)
// Возвращает: Словарь с определениями колонок
// Всегда включает: id (PRIMARY KEY), status, last
// Добавляет колонки из параметра projectColumns
// Добавляет колонки из переменной cfgToDo
// Полезно для создания стандартизированных таблиц проекта
```

---

### TblPrepareDefault
**Назначение**: Создает и подготавливает таблицу проекта по умолчанию со стандартной структурой.

**Пример**:
```csharp
// Создает таблицу с колонками id, status, last и диапазоном
project.TblPrepareDefault();
```

**Разбор**:
```csharp
public static void TblPrepareDefault(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    bool log = false                        // Включить логирование
)
// Создает таблицу используя переменную projectTable
// Добавляет стандартные колонки (id, status, last)
// Добавляет колонки из переменной cfgToDo
// Заполняет таблицу диапазоном из переменной rangeEnd
```

---

### PrepareProjectTable
**Назначение**: Создает или обновляет таблицу проекта с пользовательскими колонками и опциями.

**Пример**:
```csharp
var columns = new List<string> { "email", "username", "proxy" };
project.PrepareProjectTable(columns, "users", prune: true);
```

**Разбор**:
```csharp
public static void PrepareProjectTable(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    List<string> projectColumns = null,     // Пользовательские колонки для включения
    string tblName = null,                  // Имя таблицы (по умолчанию: переменная projectTable)
    bool log = false,                       // Включить логирование
    bool prune = false,                     // Удалить колонки, не входящие в структуру
    bool rearrange = false                  // Переупорядочить колонки в соответствии со структурой
)
// Создает таблицу, если она не существует
// Добавляет отсутствующие колонки
// Заполняет диапазоном при необходимости
// Опционально удаляет лишние колонки (prune)
// Опционально переупорядочивает колонки (rearrange)
```

---

### ClmnExist
**Назначение**: Проверяет, существует ли колонка в таблице.

**Пример**:
```csharp
if (project.ClmnExist("email", "users"))
{
    // Колонка существует
}
```

**Разбор**:
```csharp
public static bool ClmnExist(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    string clmnName,                        // Имя колонки для проверки
    string tblName,                         // Имя таблицы
    bool log = false                        // Включить логирование
)
// Возвращает: true если колонка существует, иначе false
// Регистронезависимо для PostgreSQL
```

---

### ClmnAdd
**Назначение**: Добавляет новую колонку в таблицу, если она не существует.

**Пример**:
```csharp
// Добавить одну колонку
project.ClmnAdd("email", "users");

// Добавить с пользовательским типом
project.ClmnAdd("age", "users", defaultValue: "INTEGER DEFAULT 0");

// Добавить несколько колонок из структуры
var structure = new Dictionary<string, string> {
    {"email", "TEXT DEFAULT ''"},
    {"phone", "TEXT DEFAULT ''"}
};
project.ClmnAdd(structure, "users");
```

**Разбор**:
```csharp
public static void ClmnAdd(
    this IZennoPosterProjectModel project,       // Экземпляр проекта ZennoPoster
    string clmnName,                             // Имя колонки
    string tblName = null,                       // Имя таблицы (по умолчанию: переменная projectTable)
    bool log = false,                            // Включить логирование
    string defaultValue = "TEXT DEFAULT ''"      // Тип колонки и значение по умолчанию
)
// Не делает ничего, если колонка уже существует
// Автоматически экранирует имя колонки
```

---

### ClmnDrop
**Назначение**: Удаляет колонку из таблицы.

**Пример**:
```csharp
// Удалить одну колонку
project.ClmnDrop("old_email", "users");
```

**Разбор**:
```csharp
public static void ClmnDrop(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    string clmnName,                        // Имя колонки для удаления
    string tblName,                         // Имя таблицы
    bool log = false                        // Включить логирование
)
// Не делает ничего, если колонка не существует
// Добавляет CASCADE для PostgreSQL для удаления зависимых объектов
```

---

### ClmnPrune
**Назначение**: Удаляет все колонки, не присутствующие в структуре таблицы.

**Пример**:
```csharp
var structure = new Dictionary<string, string> {
    {"id", "INTEGER PRIMARY KEY"},
    {"email", "TEXT DEFAULT ''"}
};
// Удаляет все колонки кроме id и email
project.ClmnPrune(structure, "users");
```

**Разбор**:
```csharp
public static void ClmnPrune(
    this IZennoPosterProjectModel project,       // Экземпляр проекта ZennoPoster
    Dictionary<string, string> tableStructure,   // Желаемая структура таблицы
    string tblName,                              // Имя таблицы
    bool log = false                             // Включить логирование
)
// Удаляет любую колонку, не входящую в tableStructure
// Полезно для очистки старых/неиспользуемых колонок
// Используйте с осторожностью - удаляет данные безвозвратно
```

---

### ClmnRearrange
**Назначение**: Переупорядочивает колонки таблицы в соответствии с указанной структурой.

**Пример**:
```csharp
var structure = new Dictionary<string, string> {
    {"id", "INTEGER PRIMARY KEY"},
    {"email", "TEXT DEFAULT ''"},
    {"status", "TEXT DEFAULT ''"}
};
project.ClmnRearrange(structure, "users");
```

**Разбор**:
```csharp
public static void ClmnRearrange(
    this IZennoPosterProjectModel project,       // Экземпляр проекта ZennoPoster
    Dictionary<string, string> tableStructure,   // Желаемый порядок колонок
    string tblName,                              // Имя таблицы
    bool log = false                             // Включить логирование
)
// Создает временную таблицу с новым порядком колонок
// Копирует все данные во временную таблицу
// Удаляет оригинальную таблицу и переименовывает временную
// Сохраняет тип колонки id и первичный ключ
// Выбрасывает: Exception если имя таблицы недопустимо или операция не удалась
```

---

### AddRange
**Назначение**: Вставляет записи ID в таблицу до указанного диапазона.

**Пример**:
```csharp
// Добавить ID 1-100
project.AddRange("users", 100);

// Добавить до переменной rangeEnd
project.AddRange("users");
```

**Разбор**:
```csharp
public static void AddRange(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    string tblName,                         // Имя таблицы
    int range = 0,                          // Максимальный ID (по умолчанию: переменная rangeEnd)
    bool log = false                        // Включить логирование
)
// Вставляет отсутствующие записи ID от (текущий макс + 1) до range
// Использует ON CONFLICT DO NOTHING для избежания дубликатов
// По умолчанию range=10, если переменная rangeEnd пуста
```

---

### MigrateAllTables
**Назначение**: Мигрирует все таблицы между базами данных SQLite и PostgreSQL.

**Пример**:
```csharp
// Мигрирует все таблицы на основе текущего DBmode
project.MigrateAllTables();
```

**Разбор**:
```csharp
public static void MigrateAllTables(
    this IZennoPosterProjectModel project   // Экземпляр проекта ZennoPoster
)
// Определяет направление миграции из переменной DBmode
// Если DBmode - PostgreSQL, мигрирует в SQLite
// Если DBmode - SQLite, мигрирует в PostgreSQL
// Мигрирует структуры таблиц и все данные
// Логирует прогресс и результаты
// Выбрасывает: ArgumentException если DBmode не PostgreSQL или SQLite
```

---

## Класс DbCore

### DbQ
**Назначение**: Выполняет сырые SQL запросы к базе данных.

**Пример**:
```csharp
// SELECT запрос
string result = project.DbQ("SELECT email FROM users WHERE id = 1");

// UPDATE запрос
project.DbQ("UPDATE users SET status = 'active' WHERE id = 1");

// С логированием
project.DbQ("SELECT * FROM users", log: true);
```

**Разбор**:
```csharp
public static string DbQ(
    this IZennoPosterProjectModel project,  // Экземпляр проекта ZennoPoster
    string query,                           // SQL запрос для выполнения
    bool log = false,                       // Включить логирование с префиксом emoji
    string sqLitePath = null,               // Путь к базе данных SQLite (по умолчанию: переменная DBsqltPath)
    string pgHost = null,                   // Хост PostgreSQL (по умолчанию: localhost)
    string pgPort = null,                   // Порт PostgreSQL (по умолчанию: 5432)
    string pgDbName = null,                 // База данных PostgreSQL (по умолчанию: postgres)
    string pgUser = null,                   // Пользователь PostgreSQL (по умолчанию: postgres)
    string pgPass = null,                   // Пароль PostgreSQL (по умолчанию: переменная DBpstgrPass)
    bool thrw = false,                      // Выбрасывать исключения при true
    bool unSafe = false                     // Разрешить небезопасные операции
)
// Возвращает: Результат запроса в виде строки
//   - Для SELECT: отформатированный результат с разделителями
//   - Для других запросов: количество затронутых строк
// Автоматически определяет тип запроса (SELECT vs. операции записи)
// Логирует с эмодзи для конкретной базы данных (🐘 для PostgreSQL, SQLite для SQLite)
// Возвращает пустую строку при ошибке, если только thrw=true
```

---
